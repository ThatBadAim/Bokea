# Tasks - Get Your Life Sorted

- [x] **1. SQLite & EF Core Database Layer (DbAgent)**
  - [x] Model core entities (`TaskItem`, `TaskCompletionLog`, `Sector`)
  - [x] Implement EF Core DbContext (`AppDbContext.cs`) and configure SQLite
  - [x] Seed default tasks for 4 sectors (Health, Career, Relationships, Mind)
  - [x] Auto-initialize database on application startup
- [x] **2. Background Watchdog Service (WatchdogAgent)**
  - [x] Implement `TaskWatchdogService` (`IHostedService`)
  - [x] Build math rules for State transitions (Green, Amber, Red) based on elapsed time ratio and due dates
  - [x] Update tasks transactionally in database periodically
- [x] **3. "Complete vs. Snooze" Operations (EndpointsAgent)**
  - [x] Implement endpoint `POST /api/tasks/{id}/complete` to complete task and log history
  - [x] Implement endpoint `POST /api/tasks/{id}/snooze` to delay alert without resetting history
  - [x] Implement GET/POST/PUT endpoints for managing tasks (adding, listing, editing)
- [x] **4. Aggregated Morning Digest (DigestAgent)**
  - [x] Implement `DigestService` to group Amber/Red tasks
  - [x] Expose `GET /api/digest` returning structured digest payload
  - [x] Expose API to dismiss/snooze or trigger actions on digest items
- [x] **5. UI & Analytics Interface (UiAgent)**
  - [x] Modify `index.html` to restructure the layout
  - [/] Modify `style.css` to add grid and alignment stylessmorphism, responsive grid)
  - [x] Build interactive task board with filter by sector/state
  - [x] Build Morning Digest panel alerting users of Amber/Red tasks
  - [x] Build Analytics dashboard with SVG-based consistency charts showing historical success rate
  - [x] Build custom Task Creation/Editing modals
